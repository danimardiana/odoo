<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Inherit Form View to add field of invoice policy on sale order -->
    <record id="sale_order_form_view_inherit_ivoice_policy" model="ir.ui.view">
        <field name="name">sale.order.form.view.inherit.invoice.policy</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="clx_invoice_policy_id"
                       attrs="{'readonly': [('state', 'not in', ['draft', 'sent'])]}"
                       required="True"/>
            </xpath>
        </field>
    </record>
</odoo>
