<?xml version="1.0" encoding="utf-8" ?>
<odoo>
        <!--Email template -->
        <record id="sale_order_clx_email_template" model="mail.template">
            <field name="name">Sales Order: CLX email template</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="subject">${object.company_id.name} ${object.state in ('draft', 'sent') and 'Quotation' or 'Order'} (Ref ${object.name or 'n/a' })</field>
            <!-- <field name="partner_to">${object.partner_id.account_user_id.id}</field> -->
            <field name="email_to">${object.partner_id.account_user_id.email}</field>
            <!--  -->
            <field name="model_id" eval="336"/>
            <!-- 336 - model id for sales.order -->
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.partner_id.lang}</field>            
            <field name="report_name">${(object.name or '').replace('/','_')}</field>
            <field name="report_template" eval="388"/>
            <field name="body_html"><![CDATA[
               <div style="font-size:13px;font-family:&quot;Lucida Grande&quot;, Helvetica, Verdana, Arial, sans-serif;margin:0px;padding: 0px;">
    <p style="margin:0px;font-size:13px;font-family:&quot;Lucida Grande&quot;, Helvetica, Verdana, Arial, sans-serif;padding: 0px; font-size: 13px;">
        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
        Hello,
        <br><br>
        Your Conversion Logix quotation has been prepared. Please review the attached file for <strong style="font-weight:bolder;">${object.partner_id.name}</strong> in the amount of <strong style="font-weight:bolder;">${format_amount(object.amount_total, object.pricelist_id.currency_id)}</strong>. 
        <br>Please press <strong style="font-weight:bolder;">Accept & Sign</strong> in order to review, e-sign, and submit confirmation for the community’s campaign. 
        <br>If additional changes are made, your account manager is attached to this email and can make any needed edits. 
        <br>
        Thank you, 
        <br><br>
        The Conversion Logix Team 
        <br>
    </p>
</div>
            ]]></field>
        </record>
<!-- Change the mail form template to eleminate the followers message  -->
        <record model="ir.ui.view" id="email_compose_form_sale_order_form">
            <field name="name">mail.compose.message.form.sale_order_form</field>
            <field name="model">mail.compose.message</field>
            <field name="inherit_id" ref="mail.email_compose_message_wizard_form"/>
            <field name="arch" type="xml">
                <xpath expr="//span[@name='document_followers_text']" position="replace">

                </xpath>
                <xpath expr="//field[@name='partner_ids']" position="attributes">
                    <attribute name="options">{'no_create_edit': True, 'no_create':True, 'no_open':True}</attribute>
                    <attribute name="domain">[('company_type','=','person')]</attribute>
                </xpath>                
            </field>
        </record>
</odoo>