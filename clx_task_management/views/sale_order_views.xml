<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="view_order_form_inherited_clx_task_management" model="ir.ui.view">
        <field name="name">sale.order.form.view.inherited.clx.subscription.creation</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/tree/field[@name='product_uom_qty']" position="before">
                <field name="order_id" invisible="1"/>
                <field name="order_partner_id" invisible="1"/>
                <field name="project_id"
                       domain="[('partner_id','=',order_partner_id)]"/>
                <field name="task_id" domain="[('project_id', '=', project_id),('clx_sale_order_id','=',False)]"/>
                <field name="is_qty_readonly" invisible="1"/>
            </xpath>
            <xpath expr="//field[@name='order_line']/tree/field[@name='product_uom_qty']" position="attributes">
                <attribute name="attrs">{'readonly': [('is_qty_readonly', '=', True)]}</attribute>
            </xpath>
        </field>
    </record>
</odoo>